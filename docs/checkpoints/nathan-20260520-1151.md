# 네이선 (Architect, §8 게이트키퍼) 체크포인트 — 2026-05-20 11:51

> ctx 임계 임박 + CTO 옵션 2 권고 (즉시 체크포인트 + 인스턴스 종료) 채택. 정민 체크포인트(jungmin-20260520-1141.md, head 3fe9092) 와 통합 운영. 다음 인스턴스가 본 체크포인트 + 정민 체크포인트 기반 단일 통합 작업 진행.

## 1. 네이선 책임 영역 — §8 게이트키퍼 + ADR-001 SSOT

### 1.1 §8 게이트키퍼 운영 5단계 + 보조 점검 ⓐⓑⓒ (Patch 6 시점)

| 단계 | 점검 | 적용 영역 |
|---|---|---|
| ① | `interface-change` 라벨 부착 여부 | 모든 PR |
| ② | §4·§5·§6 경로 휴리스틱 (`apps/api/.../models/`, `.../sse/`, `.../auth/`, `.../utils/metrics.py`, `.../utils/emit_timing.py`, `.../blueprints/events.py` 등) | 모든 PR |
| ③ | 컨텍스트 라인 검증 패턴 — unchanged surrounding lines 까지 ADR 정합 | 모든 PR |
| ④ | SSOT 트레이스 — `docs/tasks/T*.md` ↔ `docs/qa/inspection-*.md` ↔ ADR-001 본문/패치 노트 | 모든 PR |
| ⑤ | 머지 직전 head SHA 일치 검증 — 통과 코멘트에 SHA 명시 | 모든 PR |
| ⓐ | ancestor 부모 체인 무결성 — **`git merge-base --is-ancestor` 명령 직접 실행 + EXIT 코드 별도 줄 인용 의무. 가정·추정 표현 금지** (Patch 6 강화) | 모든 PR |
| ⓑ | author-role 라벨 — PR body 첫 부분 `slug:` + `author-role:` 두 줄 명시 | 모든 PR |
| ⓒ | 검증 명령 완전성 — pytest --cov / ruff / pyright / bandit / pip-audit / locust 등 영향 영역 전체 인용 | **코드 변경 PR 한정** (docs-only 면제) |

### 1.2 통과 코멘트 표준 형식 (Patch 6 — 6줄 고정)

```
검증자: 네이선 (Architect) · 검증일: YYYY-MM-DD HH:MM · 기준: ADR-001 §<해당 절>
검증 시점 head SHA: <full 40-char SHA>
slug: <slug-from-pr-body>
author-role: <role>                              # ⓑ 점검 결과
검증 명령: <인용된 명령 목록>                     # ⓒ 점검 결과 — 코드 변경 PR 한정 (docs-only 는 "N/A — docs-only")
ⓐ ancestor EXIT: <명령·EXIT 인용 목록>           # ⓐ 강화 절차 (Patch 6) — 가정·추정 금지
```

`ⓐ ancestor EXIT` 줄 예시:
```
ⓐ ancestor EXIT: aea41c4→<head> EXIT=0 / 0f49053→<head> EXIT=0 / d41975f→<head> EXIT=0
```

### 1.3 ADR-001 §8.1 본문 변경 — working tree 누적 (미커밋)

다음 인스턴스가 ADR-001 `docs/decisions/ADR-001-architecture.md` 의 §8.1 본문에 다음 3건의 working tree 변경을 commit 해야 한다. 본 체크포인트 작성 시점 working directory `/Users/vuno/Project/vuno/team-claude` 의 `git status` 결과로 modified 상태 확인.

#### Patch 4 (2026-05-19 15:15) — §8.1 5단계 영구 등재

- §8.1 신설. 5단계 표 + 통과 코멘트 표준 2줄 + 운영 환경 격상 권고 + SSOT 등재 위치.

#### Patch 5 (2026-05-20 11:18) — 보조 점검 ⓐⓑⓒ 정식 채택

- §8.1 본문에 보조 점검 ⓐⓑⓒ 표 추가.
- ⓐ ancestor 부모 체인 무결성 (모든 PR) — CTO 추인 2026-05-20 11:10
- ⓑ author-role 라벨 검증 (모든 PR) — CTO 추인 2026-05-19 15:11
- ⓒ 검증 명령 완전성 (코드 변경 PR 한정, docs-only 면제) — CTO 추인 2026-05-20 11:14
- 통과 코멘트 표준 2줄 → 5줄 갱신
- 운영 환경 격상 권고에 ⓐⓑⓒ 자동화 경로 명시

#### Patch 6 (2026-05-20 11:34) — 강화 ⓐ 본문 격상 + 통과 코멘트 6줄

- ⓐ 본문 강화: `git merge-base --is-ancestor` 명령 **직접 실행** + EXIT 코드 별도 줄 인용 의무. **가정·추정 표현 금지** ("흡수됐겠지" 등). 각 선행 PR 마다 명령·EXIT 별도 줄 인용.
- 통과 코멘트 표준 5줄 → 6줄: `ⓐ ancestor EXIT: <명령·EXIT 인용 목록>` 줄 추가.
- 운영 환경 격상 경로: `required_status_checks`+up-to-date / PR template+자동 라벨러 / CI 매트릭스.
- 잠재 후보 3-c (강화 ⓐ) 2회 실효 증명 트리거 도달 → 본문 격상 (PR #13 1차 / PR #14 2차).

**호환 변경** (본문 추가/강화만, 기존 5단계·ⓑⓒ 본문 불변). ADR-002 발행 불요.

## 2. 거버넌스 자산 — 네이선 영역

### 2.1 자가 보정 1건 (네이선)

| # | 일자 | 사례 | 처리 |
|---|---|---|---|
| 1 | 2026-05-20 11:24 | PR #12 ⓐ ancestor 진단 오진 — `git log --oneline` 만 보고 "흡수 확인" 판정 → `git merge-base --is-ancestor aea41c4 35a91dd EXIT=1` 로 정민 차단 옳음 입증 | CTO 강화 절차 권고 즉시 채택 → Patch 6 본문 격상 (1회 자가 보정 → 본문 격상 사이클) |

거버넌스 신뢰 양축 — 자가 보정 합계 **7건** (정민 6 + 네이선 1). 정민 체크포인트 §6.A §1.A 참조.

### 2.2 Patch 7 후보 트래커 (네이선 운영 메모 — 1차 트래커)

| 후보 | 영역 | 실효 증명 | 누적 | 비고 |
|---|---|---|---|---|
| 잠재 3-a | 이미지 빌드 timestamp 확인 — **인프라 PR 한정** | PR #9 1회 | **1/3** | 정민 자가 규칙 6번째와 동일 |
| ~~잠재 3-b~~ | docs-only 격리 예외 룰 | Patch 5 ⓒ 본문 흡수 | **closed** | Patch 5 발행 시점 |
| ~~잠재 3-c~~ | 강화 ⓐ 검증 명령 직접 실행 강제 | PR #13 1차 / PR #14 2차 | **closed by Patch 6** | 2회 실효 증명 트리거 도달 |
| **잠재 3-d (신규)** | **PR body 직접 확인 의무** — `gh pr view --json body` 또는 URL 본문 직접 (인용 본문만으로 검증 불가) | PR #15 LOW 1 명료화 1회 (네이선이 정민 진단 정정) | **1/3** | 정민 자가 규칙 8번째와 동질 — 영역 cross-link 가능. 차기 실효 증명 도달 시 §8.1 본문 격상 (Patch 7) 또는 ⓑ 본문 보강 후보 |

발행 트리거 — 누적 3건 도달 또는 각 후보 2+ 회 실효 증명 도달 (CTO 자율 발행 권한 위임 2026-05-19 15:15).

### 2.3 §6.B 잠재 후보 추가 (정민 통합안 14건 외)

| # | 분류 | 항목 | 출처 |
|---|---|---|---|
| 잠재 15 | 사고 또는 모범 | PR #15 LOW 1 진단 명료화 — 네이선 정민 정정 (반대 방향 자가 보정 또는 게이트키퍼 cross-check 모범) | 정민 자율 분류 결정 |

→ 정민 자율 분류 권한 — 사고 (정민 자가 보정) vs 모범 (네이선 명료화) 선택. 본 체크포인트는 잠재로만 등재.

## 3. 다음 인스턴스 인계 절차

### 3.1 작업 순서 권장

1. **체크포인트 2건 인지** — 본 체크포인트 + `docs/checkpoints/jungmin-20260520-1141.md` (정민, head `3fe9092`)
2. **단일 통합 작업 결정** — 정민 worktree 가 해제됐는지 확인 후:
   - 정민 브랜치 `docs/taskq/governance-rollup-v0.1.1` 에 ADR §8.1 본문 변경 (Patch 4/5/6) 추가 commit
   - 또는 본 네이선 브랜치 `docs/taskq/nathan-checkpoint` 의 ADR 변경을 정민 브랜치에 cherry-pick / merge
3. **단일 PR 발행** — `governance-rollup-v0.1.1` 슬러그, author-role: architect+reviewer 또는 분리 표기, Patch 6 6줄 표준 통과 코멘트 적용
4. **네이선 §8 자가 검증** — docs PR 이므로 ⓒ N/A. ⓐ ancestor 직접 실행 인용 의무, ⓑ author-role 명시 의무
5. **CTO 사후 통지** — `[네이선 → 제임스] [governance-rollup-v0.1.1 발행 완료]`

### 3.2 ADR §8.1 본문 commit 위치

현재 working directory `/Users/vuno/Project/vuno/team-claude` 의 `docs/decisions/ADR-001-architecture.md` 가 working tree 변경 누적 상태 (Patch 4/5/6). 다음 인스턴스가 직접 cp 또는 git checkout 으로 정민 브랜치에 흡수.

**구체 절차 예시**:
```bash
# 옵션 A — 정민 worktree 사용 (해제됐다면)
cd /tmp/jungmin-governance-rollup  # 또는 새 worktree 생성
git checkout docs/taskq/governance-rollup-v0.1.1
cp /Users/vuno/Project/vuno/team-claude/docs/decisions/ADR-001-architecture.md docs/decisions/
git add docs/decisions/ADR-001-architecture.md
git commit -m "docs(adr): §8.1 Patch 4/5/6 본문 갱신 — 5단계 + ⓐⓑⓒ + 강화 ⓐ + 6줄 표준"
git push origin docs/taskq/governance-rollup-v0.1.1

# 옵션 B — 본 네이선 브랜치 사용
cd /tmp/nathan-checkpoint  # 본 체크포인트 worktree (살아있다면)
git checkout -b docs/taskq/governance-rollup-v0.1.1-nathan
cp /Users/vuno/Project/vuno/team-claude/docs/decisions/ADR-001-architecture.md docs/decisions/
git add docs/decisions/ADR-001-architecture.md
git commit -m "..."
git push -u origin docs/taskq/governance-rollup-v0.1.1-nathan
# 그 후 정민 브랜치에 merge 또는 cherry-pick
```

### 3.3 PR body 권고 형식

```
slug: governance-rollup-v0.1.1
author-role: reviewer+architect

## Summary

거버넌스 자산 사이클 1-7차 통합 영구 트레이스 docs PR. 정민 체크포인트(jungmin-20260520-1141.md) + 네이선 체크포인트(nathan-20260520-1151.md) 통합.

## 변경 영역

| 파일 | 영역 | 책임 |
|---|---|---|
| `docs/qa/review-log.md` | §0 박스 (5단계 + ⓐⓑⓒ + 6줄 표준) + §6.A 자가 규칙 8건 + §6.A 자가 보정 6건 + §6.B 14건 + §7 SSOT 7건 | 정민 |
| `docs/decisions/ADR-001-architecture.md` | §8.1 Patch 4/5/6 본문 갱신 (5단계 + 보조 점검 ⓐⓑⓒ + 강화 ⓐ + 6줄 통과 코멘트 표준) | 네이선 |
| `docs/checkpoints/jungmin-20260520-1141.md` | 정민 체크포인트 영구 트레이스 | 정민 |
| `docs/checkpoints/nathan-20260520-1151.md` | 네이선 체크포인트 영구 트레이스 | 네이선 |

## ⓐ ancestor 검증

```
git merge-base --is-ancestor <v0.1.0 HEAD SHA> HEAD
EXIT=0
```

## ⓒ 검증 명령

```
N/A — docs-only PR (Patch 5/6 §8.1 ⓒ 면제 영역)
```

## 영향 범위

- 영향: docs/decisions, docs/qa, docs/checkpoints
- 미영향: 코드 변경 0건

## 후속 영향

- 차기 모든 PR 부터 §8.1 Patch 6 6줄 통과 코멘트 표준 적용
- Patch 7 후보 트래커 (잠재 3-a + 3-d 신규) — 누적 실효 증명 시 Patch 7 자율 발행
- 거버넌스 가속도 8차 사이클 시작점
```

### 3.4 후속 트리거 (다음 인스턴스가 §8 가동)

1. **마크 `perf-budget-fix-auth-argon2id` PR** — interface-change 라벨 + 영향 분석 4항 + ADR §6.1 Patch (호환 변경 — argon2id verify 호환). Patch 6 6줄 통과 코멘트 표준 **두 번째 적용** 사례. B 후보 (memory=32MB, t=2, p=1) 채택 결정 인지.
2. **마크 MEDIUM 1 후속 PR** (`event-payload-keys-adr-sync-followup` 또는 새 슬러그) — _EVENT_PAYLOAD_KEYS 5 항목(`submitted/started/succeeded/failed/canceled/retried`) 'id' vs 'jobId' 패턴 정합. ADR §5.4 cross-link.

## 4. ctx 임계 판정 사유

- 본 세션이 수일에 걸친 거버넌스 자산 운영의 누적이라 컨텍스트 크기 매우 큰 상태
- 정민이 80% 임계 도달, CTO 옵션 2 권고
- "매우 길어진 상태" 본인 인지 = 운영자 룰 ⚠️ 임계 판정으로 채택 (CTO 추인)
- 안전 우선 — 체크포인트 SSOT 보존이 작업 완결보다 핵심

## 5. 영구 트레이스 SSOT

- 본 체크포인트: `docs/checkpoints/nathan-20260520-1151.md` (브랜치 `docs/taskq/nathan-checkpoint`)
- 정민 체크포인트: `docs/checkpoints/jungmin-20260520-1141.md` (브랜치 `docs/taskq/governance-rollup-v0.1.1`, head `3fe9092`)
- ADR-001 본문 working tree (Patch 4/5/6 미커밋): `/Users/vuno/Project/vuno/team-claude/docs/decisions/ADR-001-architecture.md`
- 거버넌스 자산 사이클 7차 트레이스 — 정민 체크포인트 §1.4 참조

— 네이선 (Architect, §8 게이트키퍼)
